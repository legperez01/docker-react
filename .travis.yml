sudo: required
services: 
    - docker

before_install:
    - docker build -t legperez/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true legperez/docker-react npm run test -- --coverage
